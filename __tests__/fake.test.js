// fake.test.js

describe('Fake Test Suite', () => {
    test('should pass without any real logic', () => {
      expect(true).toBe(true); // This test will always pass
    });
  
    test('should also pass', () => {
      expect(1 + 1).toBe(2); // Another passing test
    });
  });
  